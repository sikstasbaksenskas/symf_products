{% extends 'base.html.twig' %}

{% block title %}Product List{% endblock %}

{% block body %}
<div class="wrapper">
    <h1>Products</h1>
    {% if products|length %}
        <ul>
            {% for product in products %}
                <li>{{ product.title }}: {{ product.price }} € 
                    <a href="{{ path('shop_product', {'id': product.id }) }}"> >>Product page<< </a>
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <div>No products</div>
    {% endif %}
</div>
{% endblock %}
